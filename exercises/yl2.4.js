const readline = require('readline').createInterface({
	input: process.stdin,
	output: process.stdout,
});

readline.question('Kas soovite istekohta valida ("ise") voi loosida ("loos")? ', istekoht => {
		function getRandom() {
			return Math.random();
		}	
		if (istekoht == 'ise') {
			readline.question('Kas soovite istuda akna ares voi mitte? ', )
		}	
		}
		let result
	})
	readline. close ()

})
 